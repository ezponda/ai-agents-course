# Python AI Agents course — supported version ranges (last verified mid-2026).
# Ranges cap at the next major so a breaking release can't land mid-lesson.
# Each notebook also installs what it needs in its first cell;
# this file installs a compatible local environment; it is not a lockfile.

# --- Block 0: pure Python core (verified against openai 2.x) ---
openai>=2,<3
pydantic>=2.0

# --- Block 1: PydanticAI (verified against 2.5) ---
pydantic-ai-slim[openai]>=2.0,<3.0
pydantic-evals>=2.0,<3.0
# pydantic-ai-slim[logfire]>=2.0,<3.0   # optional: observability (notebook 1.6)

# --- Block 2: LangChain + LangGraph (verified against 1.3 / 1.2 / 1.3) ---
langchain>=1.3,<2.0
langgraph>=1.2,<2.0
langgraph-checkpoint-sqlite>=2.0,<4.0
langchain-openai>=1.3,<2.0

# --- Data analysis project (notebook 32) ---
pandas>=2,<3

# --- RAG (notebooks 1.7, 2.7): local embeddings, no API key ---
sentence-transformers>=3.0,<6

# --- Real-tool + web (notebook 1.2) / MCP client (1.8) ---
httpx>=0.27           # already pulled in by pydantic-ai; listed for local clarity

# --- Build an MCP server (notebook 18b) ---
fastmcp>=2.0,<3.0

# --- Block 3: deploy + connect (notebooks 30, 31) ---
fastapi>=0.115         # the agent-as-API service (30); also installed inline / in the Dockerfile
uvicorn>=0.30          # ASGI server for the FastAPI app (30)
gradio>=5,<6           # runnable browser chat client for the deployed agent (31)
